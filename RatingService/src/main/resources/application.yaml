spring:
  application:
    name: RatingService
  datasource:
    url: jdbc:postgresql://localhost:5432/rating_micro_db
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8083